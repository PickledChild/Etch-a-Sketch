window.addEventListener("load", () => {
  let canvas = document.querySelector("#canvasWrap");

  canvas.style.gridTemplateColumns = "repeat(16, 1fr)";
  canvas.style.gridTemplateRows = "repeat(16, 1fr)";
});
